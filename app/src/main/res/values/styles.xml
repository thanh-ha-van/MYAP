<resources>
    <style name="Theme.Bar" parent="@style/Theme.AppCompat.Light">

    </style>

    <style name="CustomDialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">false</item>
    </style>
    AppBaseTheme
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Bar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="@style/Theme.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="style/Theme.AppCompat.Light" />

</resources>
